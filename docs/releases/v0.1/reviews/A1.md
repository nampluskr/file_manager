# A1 — phase-01 적대적 검증 (Codex)

- 검토 대상 커밋: `2015698` (phase-01 구현)
- 검증 모델: Codex CLI 기본 모델 (`gpt-5.6-terra`, `--model` 플래그 미지정)
- 실행 일시: 2026-08-14
- 실행 명령: `codex exec --sandbox read-only --cd "D:\projects\tools\file_manager" "..."`
- 심각도별 건수: Critical 0, Major 4, Minor 4

## 지적사항과 처리

| # | 심각도 | 요약 | 위반 조항 | 처리 |
|---|---|---|---|---|
| 1 | Major | 이전 `navigate` 요청이 나중에 완료되면 최신 경로 응답을 덮어쓴다 (경합) | SPEC §4.1, §4.4 | 수정. `useFilePane.ts`에 `requestIdRef` 도입, 응답 처리 시 최신 요청인지 확인 후에만 dispatch. |
| 2 | Major | OneDrive 등에서 다수 항목이 동시에 `STAT_TIMEOUT_MS`를 초과하면, timeout으로 호출자는 먼저 반환되지만 이미 시작된 `lstat` 작업 자체는 취소되지 않아 백그라운드에 계속 남는다 | SPEC §4.1, §7.1 | 보류. 현재도 목록 자체는 블로킹되지 않는다(Codex 자체 확인). 완전한 해결은 `fs/promises`에 `AbortSignal`을 연결해야 하는데, 이 앱의 취소 인프라(`opId` + `AbortController`)는 SPEC §6.8에 따라 phase-02/07의 파일 작업에서 도입 예정이다. listDirectory에도 동일 인프라를 붙이는 작업은 그때 함께 검토한다. |
| 3 | Major | 긴 UNC 경로(`\\server\share\...`)를 `\\?\` 접두사만 붙여 `\\?\\\server\share\...`로 잘못 변환한다 | SPEC §12.1 | 수정. `pathUtils.ts`의 `toLongPathSafe`가 `\\`로 시작하는 경로는 `\\?\UNC\` 형식으로 변환하도록 분기 추가. UNC 케이스 테스트 2건 추가. |
| 4 | Major | 앱 실행 직후 클릭이나 Tab 없이 방향키/Enter/Backspace를 눌러도 반응하지 않는다 (초기 포커스 없음) | SPEC §4.3 | 수정. `FilePane.tsx`에 `paneRef` + mount 시 `focus()` 추가. CDP로 클릭/포커스 호출 없이 ArrowDown이 즉시 동작함을 재확인. |
| 5 | Minor | 자연 정렬이 숫자 토큰을 `Number()`로 변환해 `Number.MAX_SAFE_INTEGER`를 넘는 큰 숫자 이름에서 정밀도 손실로 서로 다른 이름을 동일하게 취급할 수 있다 | SPEC §4.5 | 수정. `sortEntries.ts`의 `naturalCompare`를 문자열 길이 우선 비교(`compareDigits`)로 재작성해 `Number()` 변환을 제거. 큰 숫자 이름 테스트, 선행 0 동률 처리 테스트 추가. |
| 6 | Minor | timeout으로 인한 "메타데이터 알 수 없음" sentinel(size 0, mtime 0)이 실제 빈 파일/epoch mtime 파일과 화면상 구별되지 않는다 | SPEC §3.4, §7.1 | 보류. 구분하려면 `FileEntry`에 필드를 추가해야 하는데 이는 SPEC §7.1에 문서화된 확정 스키마 변경이라 `BRIEF → PRD → SPEC → PLAN → backlog` 순서의 문서 갱신이 먼저 필요하다(`CLAUDE.md` Document Rules). 실사용 빈도가 낮고(OneDrive 온디맨드 타임아웃 자체가 드묾) 되돌릴 수 없는 손상도 아니므로, SPEC 개정 여부를 사용자와 논의한 뒤 별도로 처리한다. |
| 7 | Minor | `[..]` 항목이 화면에 `..`로 표시되어 SPEC 표기와 다르다 | SPEC §3.4, §4.2 | 수정. `FileList.tsx`에서 `isParent` 항목은 표시용 이름만 `[..]`로 바꿔 렌더링(내부 `name`/포커스 매칭 로직은 그대로 `..` 유지). |
| 8 | Minor | `OverlayState`가 도입되면(F3/F4/대화상자) type-ahead가 오버레이 위로 새어 나갈 구조적 위험이 있다 — 현재 phase-01에는 overlay가 아예 없어 즉시 재현되지는 않는다 | SPEC §4.6, §16.7 | 보류. Codex 지적대로 지금은 재현 불가능한 잠재 이슈다. phase-03(Viewer)·phase-04(Editor)에서 overlay 상태를 실제로 도입할 때 `useFilePane`의 키 핸들러가 `overlay.kind !== 'none'`이면 type-ahead/이동 키를 무시하도록 가드를 추가한다. |

## 재검증

Critical 지적이 없어 재검증이 필수는 아니지만, Major 3건과 Minor 2건(정렬, `[..]` 표기)을 수정한 뒤
다음을 다시 실행해 회귀가 없음을 확인했다.

- `npm run typecheck` 통과
- `npm test` 통과 (64/64, 신규 테스트 4건 추가: UNC 긴 경로 2건, 큰 숫자 자연 정렬 2건)
- `npm run build` 산출물 생성 확인
- `npm run dev`로 실제 Electron 창을 띄우고 Chrome DevTools Protocol로 직접 검증:
  - 클릭/Tab 없이 즉시 ArrowDown이 반응함 (Major #4 해소 확인)
  - `[..]` 표시 문자열이 화면에 정확히 나타남 (Minor #7 해소 확인)

Codex 재실행은 하지 않았다 (Critical 없음, 수정 내용이 지적사항을 그대로 반영한 국소 변경).
