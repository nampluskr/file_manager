# A0 — phase-00 적대적 검증 (Codex)

- 검토 대상 커밋: `fdcba2e` (phase-00 구현)
- 검증 모델: Codex CLI 기본 모델 (`gpt-5.6-terra`, `--model` 플래그 미지정). 최초 시도는
  `--model sol`로 실행했으나 이 환경의 ChatGPT 계정 인증에서 400 오류
  (`The 'sol' model is not supported when using Codex with a ChatGPT account.`)로 거부되어,
  `workflow-hub` 프로젝트의 검증된 패턴을 따라 `--model` 없이 재실행했다 (`CLAUDE.md` 갱신,
  커밋 `222d08a`).
- 실행 일시: 2026-08-14
- 실행 명령: `codex exec --sandbox read-only --cd "D:\projects\tools\file_manager" "..."`
- 심각도별 건수: Critical 0, Major 3, Minor 2

## 지적사항과 처리

| # | 심각도 | 요약 | 위반 조항 | 처리 |
|---|---|---|---|---|
| 1 | Major | `src/main/filesystem/*`의 electron import 금지가 문서/관례뿐이며 자동 강제 장치가 없다 | SPEC §11.4, §17 | 수정. `scripts/checkFilesystemBoundary.mjs` 추가, `npm test`가 실행 전 이 스크립트를 먼저 실행하도록 배선(`npm run check:boundary && vitest run`). electron import가 있으면 종료 코드 1로 실패. 의도적으로 electron import를 주입해 실패하는 것을 확인한 뒤 원복. |
| 2 | Major | `AppState`/`OverlayState`가 Phase 7의 진행 중인 작업 상태(현재 처리 파일, 취소, 충돌 응답 보관)를 표현하지 못한다 | SPEC §7.3, §6.1, §6.6, §6.8, §11.2–§11.3 | 보류 (phase-07로 이관). SPEC 자체에도 진행 중 작업 상태 모델이 정의되어 있지 않다 — phase-00의 `adversarialFocus`가 이 공백을 지적하도록 의도된 항목이다. 지금 임의로 필드를 추가하면 phase-07 설계 이전에 추측성 스키마가 고정되어 "불필요한 추상화" 위반 소지가 있다. phase-07 착수 시 `OverlayState`/`OpResult`에 `opId` 연결과 진행 상태 필드를 추가한다. |
| 3 | Major | 스캐폴딩 채널 `app:ping`이 `shared/ipc.ts` 계약 밖에서 preload와 main이 각자 문자열/익명 타입으로 정의해 불일치를 TypeScript가 잡지 못한다 | SPEC §11.1 | 수정. `shared/ipc.ts`에 `PING_CHANNEL` 상수와 `PingResult` 타입을 정의하고 `src/main/ipc/index.ts`, `src/preload/index.ts` 양쪽에서 동일 정의를 import하도록 변경. |
| 4 | Minor | preload가 사용하지 않는 `platform`을 Renderer에 노출한다 | SPEC §11.1 | 수정. `contextBridge`에서 `platform` 제거, `preload.d.ts`도 `ping`만 남김. |
| 5 | Minor | Vitest 설정(`src/**/*.test.ts`)이 electron import 모듈에 닿았을 때 예방이 아니라 우연한 런타임 실패에 의존한다 | SPEC §11.4, §18.2, §17 | #1의 `check:boundary` 스크립트로 해소. 이 스크립트는 테스트 존재 여부와 무관하게 `src/main/filesystem/*` 소스 자체를 정적으로 스캔하므로, Vitest가 무엇을 어떻게 실패시키는지와 별개로 위반을 사전에 차단한다. |

## 재검증

Critical 지적이 없어 `phaseCompletionWorkflow`상 재검증이 필수는 아니지만, Major #1/#3과 Minor #4를
수정한 뒤 다음을 다시 실행해 회귀가 없음을 확인했다.

- `npm run typecheck` 통과
- `npm run check:boundary` 통과 (그리고 의도적 위반 주입 시 실패하는 것도 확인)
- `npm test` 통과 (8/8)
- `npm run build` 산출물 생성 확인

Codex 재실행은 하지 않았다 (Critical 없음, 위 수정은 지적사항을 문자 그대로 반영한 국소 변경이라
재해석 여지가 없다고 판단). 사용자가 원하면 재검증을 추가로 요청할 수 있다.
